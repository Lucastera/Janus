# Janus 仓库文件说明

本文档介绍 Janus 仓库中各个文件和目录的作用。

## 📋 目录结构概览

```
Janus/
├── janus/                    # 核心代码包
├── demo/                     # 演示应用
├── images/                   # 图片资源
├── inference.py              # 推理脚本
├── generation_inference.py   # 图像生成推理脚本
├── interactivechat.py        # 交互式聊天脚本
├── README.md                 # 项目说明文档
├── requirements.txt          # Python 依赖列表
├── pyproject.toml           # 项目配置文件
├── Makefile                 # 自动化构建工具
└── LICENSE-*                # 许可证文件
```

## 📁 主要目录

### `janus/` - 核心代码包

包含 Janus 模型的核心实现代码，这是项目的主要功能模块。

#### `janus/models/` - 模型实现
- **`modeling_vlm.py`** - 视觉语言模型（VLM）的主要实现，包含 MultiModalityCausalLM 类，这是统一多模态理解和生成的核心模型
- **`processing_vlm.py`** - VLChatProcessor 处理器，负责处理对话格式、图像和文本的预处理
- **`image_processing_vlm.py`** - 图像处理相关功能，用于处理输入图像
- **`siglip_vit.py`** - SigLIP 视觉编码器实现，用于多模态理解的视觉特征提取
- **`clip_encoder.py`** - CLIP 编码器实现，用于图像生成的视觉编码
- **`vq_model.py`** - VQ-VAE 模型实现，用于图像的编码和解码
- **`projector.py`** - 投影层实现，用于连接视觉编码器和语言模型
- **`__init__.py`** - 模块初始化文件，导出主要类和函数

#### `janus/utils/` - 工具函数
- **`conversation.py`** - 对话处理相关的工具函数
- **`io.py`** - 输入输出工具，包含图像加载函数 load_pil_images
- **`__init__.py`** - 工具模块初始化

#### `janus/janusflow/` - JanusFlow 模型实现

JanusFlow 是使用整流流（Rectified Flow）进行图像生成的统一模型版本。

- **`models/modeling_vlm.py`** - JanusFlow 的 VLM 实现
- **`models/processing_vlm.py`** - JanusFlow 的处理器
- **`models/uvit.py`** - U-ViT 网络实现，用于基于流的图像生成
- **其他文件** - 与 Janus 主模型类似的组件，但针对 JanusFlow 优化

### `demo/` - 演示应用

包含各种演示应用程序，用于展示模型的功能。

- **`app.py`** - Janus-1.3B 的 Gradio 网页演示应用
- **`app_januspro.py`** - Janus-Pro 的 Gradio 网页演示应用
- **`app_janusflow.py`** - JanusFlow 的 Gradio 网页演示应用
- **`fastapi_app.py`** - FastAPI 服务器实现，提供 API 接口
- **`fastapi_client.py`** - FastAPI 客户端示例，演示如何调用 API
- **`Janus_colab_demo.ipynb`** - Google Colab 笔记本演示，可在云端运行

### `images/` - 图片资源

存放项目相关的图片资源。

- **`logo.svg` / `logo.png`** - Janus 项目的标志
- **`badge.svg`** - 项目徽章
- **`teaser.png`** - Janus 模型的展示图
- **`teaser_januspro.png`** - Janus-Pro 的展示图
- **`teaser_janusflow.png`** - JanusFlow 的展示图
- **`equation.png`** - 示例图片（用于演示公式识别）
- **`doge.png`** - 示例图片
- **`ve.png`** - 视觉编码相关示意图
- **`pie_chart.png`** - 饼图示例

## 📄 主要脚本文件

### `inference.py` - 多模态理解推理脚本

用于执行多模态理解任务的示例脚本。该脚本演示了如何：
- 加载 Janus 模型
- 处理包含图像的对话
- 使用模型进行图像理解（如将图片中的公式转换为 LaTeX 代码）

**主要功能**：
- 加载预训练的 Janus-1.3B 模型
- 接收图像和文本输入
- 生成对图像内容的理解和描述

### `generation_inference.py` - 图像生成推理脚本

用于文本到图像生成任务的示例脚本。该脚本演示了如何：
- 从文本描述生成图像
- 使用分类器自由引导（Classifier-Free Guidance）提高生成质量
- 并行生成多张图像

**主要功能**：
- 根据文本提示生成图像
- 支持可配置的生成参数（温度、CFG 权重等）
- 将生成的图像保存到本地

### `interactivechat.py` - 交互式聊天脚本

提供一个交互式的命令行界面，用于与 Janus 模型进行对话和图像生成。

**主要功能**：
- 命令行交互界面
- 支持连续对话
- 支持实时图像生成

## 📝 配置和文档文件

### `README.md` - 项目说明文档

项目的主要文档，包含：
- 项目介绍和概述
- Janus、Janus-Pro 和 JanusFlow 三个模型系列的说明
- 模型下载链接
- 快速开始指南
- 安装说明
- 使用示例（多模态理解和图像生成）
- Gradio 和 FastAPI 演示说明
- 许可证信息
- 引用信息

### `requirements.txt` - Python 依赖列表

列出项目所需的 Python 包及其版本，包括：
- `torch==2.0.1` - PyTorch 深度学习框架
- `transformers>=4.38.2` - Hugging Face Transformers 库
- `timm>=0.9.16` - PyTorch 图像模型库
- `accelerate` - 加速训练和推理
- `sentencepiece` - 分词器
- `einops` - 张量操作库
- Gradio 相关依赖（用于网页演示）

### `pyproject.toml` - 项目配置文件

Python 项目的现代配置文件，定义了：
- 项目元数据（名称、版本、作者等）
- 核心依赖
- 可选依赖（gradio、lint 工具等）
- 构建系统配置
- 包结构定义

### `Makefile` - 自动化构建工具

提供自动化命令，用于：
- **代码质量检查**：
  - `make pylint` - 运行 pylint 代码检查
  - `make flake8` - 运行 flake8 风格检查
  - `make ruff` - 运行 ruff 快速检查
  - `make mypy` - 运行类型检查
- **代码格式化**：
  - `make format` - 格式化代码（使用 black 和 isort）
  - `make py-format` - 检查代码格式
- **其他**：
  - `make lint` - 运行所有代码检查
  - `make clean` - 清理临时文件
  - `make addlicense` - 添加许可证头

## 📜 许可证文件

### `LICENSE-CODE` - 代码许可证

MIT 许可证，适用于项目的源代码。这是一个宽松的开源许可证，允许：
- 自由使用、修改、分发代码
- 商业使用
- 私有使用
- 唯一要求是保留版权声明和许可证声明

### `LICENSE-MODEL` - 模型许可证

DeepSeek 模型许可协议，适用于预训练模型。该协议：
- 允许研究和商业使用
- 包含使用限制条款，防止潜在的滥用
- 要求衍生模型也要包含相同的使用限制
- 旨在平衡开放性和负责任的 AI 开发

## 📄 其他文件

### `janus_pro_tech_report.pdf` - 技术报告

Janus-Pro 的技术报告，详细介绍了：
- Janus-Pro 的架构设计
- 训练策略和数据规模
- 性能评估结果
- 与 Janus 的改进对比

### `.gitignore` - Git 忽略文件

指定 Git 版本控制应忽略的文件和目录，如：
- Python 缓存文件（`__pycache__`、`*.pyc`）
- 虚拟环境
- 生成的文件
- 临时文件

### `.gitattributes` - Git 属性文件

配置 Git 如何处理特定类型的文件，如：
- 行尾符处理
- 文件差异显示方式
- 大文件处理

## 🚀 使用场景

### 多模态理解
使用 `inference.py` 或 `demo/app.py` 进行：
- 图像问答
- 视觉推理
- OCR（光学字符识别）
- 公式识别
- 文档理解

### 图像生成
使用 `generation_inference.py` 或 `interactivechat.py` 进行：
- 文本到图像生成
- 创意图像创作
- 概念可视化
- 艺术风格迁移

### 应用开发
- 使用 `demo/fastapi_app.py` 构建 API 服务
- 使用 `demo/app*.py` 创建交互式 Web 应用
- 使用核心模块集成到自己的项目中

## 📚 模型系列说明

### Janus-1.3B
- 基础版本，1.3B 参数
- 统一的多模态理解和生成框架
- 使用解耦的视觉编码

### Janus-Pro (1B 和 7B)
- Janus 的增强版本
- 优化的训练策略
- 扩展的训练数据
- 更大的模型规模（7B 版本）
- 显著改进的性能

### JanusFlow-1.3B
- 使用整流流（Rectified Flow）的版本
- 改进的图像生成质量
- 统一的多模态架构
- 与自回归语言模型的简洁集成

## 🔧 开发工具

项目包含完整的开发工具链：
- **代码检查**：pylint、flake8、ruff、mypy
- **代码格式化**：black、isort
- **版权管理**：addlicense
- **版本控制**：git 配置
- **自动化**：Makefile 命令

## 📖 快速开始

1. **安装依赖**：
   ```bash
   pip install -e .
   ```

2. **多模态理解**：
   ```bash
   python inference.py
   ```

3. **图像生成**：
   ```bash
   python generation_inference.py
   ```

4. **启动演示**：
   ```bash
   pip install -e .[gradio]
   python demo/app.py  # Janus
   # 或
   python demo/app_januspro.py  # Janus-Pro
   # 或
   python demo/app_janusflow.py  # JanusFlow
   ```

5. **API 服务**：
   ```bash
   python demo/fastapi_app.py
   ```

## 🎯 总结

Janus 是一个功能强大的统一多模态框架，支持：
- ✅ 图像理解和问答
- ✅ 文本到图像生成
- ✅ 多种模型版本选择
- ✅ 易于使用的演示应用
- ✅ 灵活的 API 接口
- ✅ 完整的开发工具链
- ✅ 开源且商业友好的许可证

所有文件都经过精心组织，便于研究人员和开发者快速上手和集成到自己的项目中。
